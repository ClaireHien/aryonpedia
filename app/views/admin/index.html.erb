
<div class="left">
<div class="titleleft">Panneau d'administration</div>
<a href="<%= admin_user_path%>" class="navleft">Les membres</a>
<a href="<%= admin_bestiary_path%>" class="navleft">Le bestiaire</a>
<a href="<%= admin_herbarium_path%>" class="navleft">L'herbier</a>
<a href="/admin" class="aryon">Vue d'ensemble</a> 
</div>

<h1>Panneau d'administration</h1>

<br /><br />


<h2>Les statistiques</h2>
<div class="d-flex flex-nowrap">
<div style="width:400px"><h3>bestiaire</h3>

<%all_bestiary = Bestiary.all %>
Nombre de bêtes : <%= all_bestiary.length %><br /><br />

<strong>Rareté :</strong><br />
<% all_rarity = RarityBestiary.all%>
  <% all_rarity.each do |r| %>
    <%rarity = Bestiary.where(rarity_bestiary_id: r.id)%>
    <%= r.name %> : <%= rarity.length %><br />
  <%end%><br />

<strong>Habitat :</strong><br />
<% all_habitat = HabitatBestiary.all%>
  <% all_habitat.each do |h| %>
    <%habitat = Bestiary.where(habitat_bestiary_id: h.id)%>
    <%= h.name %> : <%= habitat.length %><br />
  <%end%><br />

  <strong>Dangerosité :</strong><br />
  <% all_level = Level.all%>
    <% all_level.each do |lv| %>
      <%level = Bestiary.where(level_id: lv.id)%>
      <%= lv.name %> : <%= level.length %><br />
    <%end%><br />
  
</div>
<div style="width:400px"><h3>herbier</h3>

<%all_herbarium = Herbarium.all %>
Nombre de plantes : <%= all_herbarium.length %><br /><br />

<strong>Rareté :</strong><br />
<% all_rarity = RarityHerbarium.all%>
  <% all_rarity.each do |r| %>
    <%rarity = Herbarium.where(rarity_herbarium_id: r.id)%>
    <%= r.name %> : <%= rarity.length %><br />
  <%end%><br />

<strong>Habitat :</strong><br />
<% all_habitat = HabitatHerbarium.all%>
  <% all_habitat.each do |hh| %>
    <%habitat = Herbarium.where(habitat_herbarium_id: hh.id)%>
    <%= hh.name %> : <%= habitat.length %><br />
  <%end%><br />

<strong>Saisons :</strong><br />
<% all_season = Season.all%>
  <% all_season.each do |s| %>
    <%season = Herbarium.where(season_id: s.id)%>
    <%= s.name %> : <%= season.length %><br />
  <%end%><br />

<strong>Catégories :</strong><br />
<% all_cat = Category.all%>
  <% all_cat.each do |c| %>
    <%cat = Herbarium.where(category_id: c.id)%>
    <%= c.name %> : <%= cat.length %><br />
  <%end%><br />
</div>
</div>